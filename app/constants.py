from app.models import Department, Job, Employee

# Constante para mapear todos los objetos de la base de datos
MODEL_MAP = {
    "departments": Department,
    "jobs": Job,
    "employees": Employee
}
